# Copilot Instructions

## Project Guidelines
- User prefers int IDs with auto-increment in database instead of Guid. All database schema changes and interface signatures should use int for entity IDs.